For API Endpoint: 

Open the environment.prod.ts file from the environment folder and change the apiEndpoint varaible.

Then,
install node_muldes with

npm install

Build the project with:

npm run build


After that www folder will be created, open the folder and copy the files from that and upload on server.